query Projects($slug: String!) {
  projects(where: {slug: $slug}) {
    id
    slug
    title
    date
    description
    role
    link
    github
    agency
    cover {
      url
    }
    agency
  }
}